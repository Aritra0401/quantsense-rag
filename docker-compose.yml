version: '3.8'
services:
  quantsense:
    build: .
    ports:
      - '8501:8501'
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - TAVILY_API_KEY=${TAVILY_API_KEY}
    volumes:
      - ./data:/app/data
      - ./.chroma:/app/.chroma
    restart: unless-stopped